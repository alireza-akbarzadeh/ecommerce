import { defineMessages } from 'react-intl'

const scope = 'app.commerce.hooks'
const HooksMessages = defineMessages({
  buyOrderSuccess: {
    id: `${scope}.buyOrderSuccess`,
    defaultMessage: 'افزودن به سبد خرید موفقیت آمیز بود',
  },
})

export default HooksMessages
