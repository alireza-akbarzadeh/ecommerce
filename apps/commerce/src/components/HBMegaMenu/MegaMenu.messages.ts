import { defineMessages } from 'react-intl'

const scope = 'app.commerce.components.megamenu'
const MegaMenuMessages = defineMessages({
  all: {
    id: `${scope}.all`,
    defaultMessage: 'همه محصولات',
  },
  saved: {
    id: `${scope}.saved`,
    defaultMessage: 'ذخیره شده',
  },
})

export default MegaMenuMessages
