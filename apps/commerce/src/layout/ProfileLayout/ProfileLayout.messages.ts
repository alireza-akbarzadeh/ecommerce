import { defineMessages } from 'react-intl'

const scope = 'app.commerce.profile-layout'
const ProfileLayoutMessages = defineMessages({
  cartWithCount: {
    id: `${scope}.cart`,
    defaultMessage: 'سبد خرید ({count} کالا)',
  },
})

export default ProfileLayoutMessages
