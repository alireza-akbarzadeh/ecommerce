import { defineMessages } from 'react-intl'

const tagsControllerMessages = defineMessages({
  existError: {
    id: 'app.vendor.containers.HBTagsController.existError',
    defaultMessage: 'این تگ قبلا در سیستم ثبت شده است',
  },
})
export default tagsControllerMessages
