import { defineMessages } from 'react-intl'

const hbTagsMessages = defineMessages({
  existError: {
    id: 'app.vendor.domains.hbTags.existError',
    defaultMessage: 'این تگ قبلا در سیستم ثبت شده است',
  },
})
export default hbTagsMessages
