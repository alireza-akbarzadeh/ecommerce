import { defineMessages } from 'react-intl'

const scope = 'app.vendor.containers.HBViewContainerWithTree'
export const hbViewContainerWithTreeMessages = defineMessages({
  addButton: {
    id: `${scope}.addButton`,
    defaultMessage: 'افزودن',
  },
  dataNotFound: {
    id: `${scope}.dataNotFound`,
    defaultMessage: 'نتیجه‌ای یافت نشد.',
  },
})
