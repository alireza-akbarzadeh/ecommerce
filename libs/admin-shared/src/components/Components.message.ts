import { defineMessages } from 'react-intl'

const scope = 'app.vendor.containers'
const ComponentsMessages = defineMessages({
  noResult: {
    id: `${scope}.noResult`,
    defaultMessage: 'نتیجه‌ای یافت نشد.',
  },
})

export default ComponentsMessages
