import { styled } from '@mui/material'
import { HBAuthenticationFormItemProps } from './HBAuthenticationFormItem'

export const HBAuthenticationFormItemRootStyle = styled('div')<HBAuthenticationFormItemProps>(
  ({ theme }) => ({}),
)
