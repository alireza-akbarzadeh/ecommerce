import { styled } from '@mui/material'
import { HBPasswordFormItemProps } from './HBPasswordFormItem'

export const HBPasswordFormItemRootStyle = styled('div')<HBPasswordFormItemProps>(
  ({ theme }) => ({}),
)
