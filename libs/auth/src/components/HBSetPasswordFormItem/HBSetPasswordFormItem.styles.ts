import { styled } from '@mui/material'
import { HBChangePasswordFormItemProps } from './HBSetPasswordFormItem'

export const HBChangePasswordFormItemRootStyle = styled('div')<HBChangePasswordFormItemProps>(
  ({ theme }) => ({}),
)
