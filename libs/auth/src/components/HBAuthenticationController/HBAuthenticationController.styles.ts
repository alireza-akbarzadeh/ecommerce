import { styled } from '@mui/material'
import { HBAuthenticationControllerProps } from './HBAuthenticationController'

export const HBAuthenticationControllerRootStyle = styled('div')<HBAuthenticationControllerProps>(
  ({ theme }) => ({}),
)
