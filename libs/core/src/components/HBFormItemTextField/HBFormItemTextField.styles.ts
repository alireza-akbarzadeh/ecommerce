import { styled } from '@mui/material'
import { HBFormItemTextFieldProps } from './HBFormItemTextField'

export const HBFormItemTextFieldRootStyle = styled('div')<Partial<HBFormItemTextFieldProps>>(
  ({ theme }) => ({}),
)
