import { defineMessages } from 'react-intl'

const queryBuilderMessages = defineMessages({
  rule: {
    id: `app.queryBuilder.rule`,
    defaultMessage: 'شرط',
  },
  group: {
    id: `app.queryBuilder.group`,
    defaultMessage: 'گروه',
  },
})
export default queryBuilderMessages
